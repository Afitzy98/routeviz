# Rust
/target

# Node / Vite
node_modules
/web/dist
/web/.vite

# WASM build output (regenerated by `wasm-pack build`)
/wasm/pkg

# Bench output (written by deploy workflow, never committed)
/web/public/benchmarks.json

# Editor / OS
.DS_Store
*.log
.vscode
.idea
.claude